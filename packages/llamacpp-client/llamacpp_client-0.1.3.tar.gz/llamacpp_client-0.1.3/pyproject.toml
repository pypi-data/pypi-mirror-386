[project]
name = "llamacpp-client"
version = "0.1.3"
description = "Client for LlamaCpp HTTP Server"
authors = [
    { name = "Luís Gomes", email = "lmdgomes@fc.ul.pt" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "httpx",
    "requests",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

