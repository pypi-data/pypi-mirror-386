[project]
name = "datallog"
version = "0.1.34"
description = "SDK datallog library"
authors = [
    {name = "Geovane Schmitz", email = "contato@geovanems.com.br"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
name = "datallog"
version = "0.1.34"
description = "SDK datallog library"
authors = ["Geovane Schmitz <contato@geovanems.com.br>"]
packages = [
    { include = "datallog" },
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
