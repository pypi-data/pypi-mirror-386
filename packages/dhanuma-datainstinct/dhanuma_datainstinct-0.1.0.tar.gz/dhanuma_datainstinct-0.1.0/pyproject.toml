[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dhanuma_datainstinct"
version = "0.1.0"
description = "A lightweight data profiler for CSV, Excel and JSON files"
readme = "README.md"
authors = [{ name = "Dhanush", email = "dhanushm.analyst@gmail.com" }]
dependencies = ["pandas>=1.3.0","openpyxl>=3.0.0"]
requires-python = ">=3.8"
