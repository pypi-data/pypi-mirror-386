server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        return 301 /latest;
    }

    location /latest {
       alias /usr/share/nginx/html/latest/;
       index index.html;
       try_files $uri $uri/ =404;
    }
}
