from setuptools import setup, find_packages

setup(
    name="incupie",
    version='0.1.0',
    package=find_packages(),
    install_requires=[
        # Add dependencies here.
        # e.g. 'numpy>=1.11.1'
    ],
)