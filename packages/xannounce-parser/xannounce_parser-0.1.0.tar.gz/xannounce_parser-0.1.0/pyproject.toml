[project]
name = "xannounce-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "provefars", email = "provefars@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "lark>=1.3.0",
]

[project.scripts]
xparser = "xparser:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "xparser"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
