[build-system]
requires = ["setuptools", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ethaddrlib"
version = "0.1.0"
dependencies = [
    "bip-utils",
    "eth-account",
    "eth-keys"
]