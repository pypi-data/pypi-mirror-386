project_name: mnist_example
hyperparameters:
  learning_rate: 1e-3
  batch_size: 64
  epochs: 10
flags:
  use_subset: false
  save_model: true
  debug: true
  use_wandb: false
model:
  architecture: "SimpleCNN"
  num_classes: 10
dataset:
  name: "MNIST"
  path: "./data"
wandb:
  mode: "online"  # online/offline
