[package]
name = "python"
version = "0.2.0"
edition = "2021"
publish = false
readme = "README.md"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "hardware_address"
crate-type = ["cdylib", "lib"]

[dependencies]
pyo3 = "0.27"
hardware-address = { path = "../", version = "0.2.0", features = ["pyo3"] }

[build-dependencies]
pyo3-build-config = "0.27"
