{# def title=None, title_hidden=False #}

<div class="ui section">
    {% if field_value(title) and not field_value(title_hidden) %}
    <h2 class="ui header">
        {{ title }}
    </h2>
    {% endif %}
    {{ content }}
</div>