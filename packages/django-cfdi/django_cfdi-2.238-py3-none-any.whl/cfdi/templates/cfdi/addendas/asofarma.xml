{% load cfdi_tags %}
<cfdi:Addenda>
    <ASONIOSCOC ordenCompra="{{ ordenCompra }}" folio="{{ folio }}" serie="{{ serie }}" noProveedor="{{ noProveedor }}" tipoProveedor="{{ tipoProveedor }}">
        <Partidas>
            <Partida Otros="{{ otros }}" ivaDevengado="{{ ivaDevengado }}" ivaAcreditable="{{ ivaAcreditable }}" noPartida="{{ noPartida }}"/>
        </Partidas>
    </ASONIOSCOC>
</cfdi:Addenda>
