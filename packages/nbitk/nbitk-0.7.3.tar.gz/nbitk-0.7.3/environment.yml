# This conda environment.yml is here so that the CI pipeline can also test tool wrappers.
# In addition to the pyproject.toml it forms the basis of the full list of dependencies on bioconda:
# https://github.com/bioconda/bioconda-recipes/blob/master/recipes/nbitk/meta.yaml 
name: nbitk
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.12
#  - blast
#  - hmmer
#  - raxml-ng
#  - perl-bio-phylo-forest-dbtree
#  - vsearch
  - pip
  - pip:
    - -e .  # This will install your package and dependencies from pyproject.toml

