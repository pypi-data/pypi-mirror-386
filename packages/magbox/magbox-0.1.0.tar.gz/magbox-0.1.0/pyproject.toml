[project]
name = "magbox"
version = "0.1.0"
description = "Solve lattice llg numerically"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "torch>=2.8.0",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["uv_build >= 0.9.2, <0.10.0"]
build-backend = "uv_build"