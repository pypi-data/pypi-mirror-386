TYPE1:
  METRIC1: 
    metric_query: METRIC1("{{ col_name }}")
    metric_return_datatype: TYPE1

  METRIC2:
    metric_query: METRIC2("{{ col_name }}")
    metric_return_datatype: TYPE2

TYPE2:
  METRIC1:
    metric_query: METRIC1("{{ col_name }}")
    metric_return_datatype: TYPE3
