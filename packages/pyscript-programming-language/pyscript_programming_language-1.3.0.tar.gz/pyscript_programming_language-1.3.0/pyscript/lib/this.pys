try {
    __f = open(pyimport('os').path.join(require('sys').pyscript_path, 'this.py'), 'r')
    __l = {}

    try {
        pyimport('builtins').exec(__f.read(), globals={}, locals=__l)
        s = __l['s']
    }

    finally {
        __f.close()
        del [__f, __l]
    }
}

catch {
    s = "Uh uh, something it's wrong!!11!!"
    print(s)
}