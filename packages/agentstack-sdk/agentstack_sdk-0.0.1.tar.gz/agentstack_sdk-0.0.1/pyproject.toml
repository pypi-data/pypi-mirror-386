[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentstack-sdk"
version = "0.0.1"
description = "reserve agentstack-sdk"
authors = [{ name = "IBM Corp." }]
readme = "README.md"
requires-python = ">=3.11"