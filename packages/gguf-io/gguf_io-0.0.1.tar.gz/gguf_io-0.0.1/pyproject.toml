[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"

[project]
name = "gguf-io"
authors = [{name = "gguf", email = "info@gguf.us"}]
description = "localhost for gguf.org frontend"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]

[project.urls]
Repository = "https://github.com/gguf-org/frontend"