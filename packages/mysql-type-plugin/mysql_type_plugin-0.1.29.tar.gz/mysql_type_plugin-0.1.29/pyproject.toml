[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
python-source = "."
module-name = "mysql_type_plugin"

# [tool.maturin]
# name = ""
# requires-python = ">=3.9"
# classifiers = [
#     "Programming Language :: Python",
#     "Programming Language :: Rust"
# ]

