# yaml-language-server: $schema=https://apps.kelvininc.com/schemas/kelvin/{{ spec_version }}/{{ app_type }}.json

# YAML (Structure) Spec Version
spec_version: {{ spec_version }}
# Type
type: {{ app_type }}

# Info
name: {{ app_name }}
title: {{ title }}
description: {{ app_description }}
version: {{ app_version }}

# Capabilities
flags:
  enable_runtime_update:
    # Reload Application Configuration in runtime
    configuration: false

# Dynamic IO Definition (Output Types)
importer_io:
  - name: default
    data_types:
      - number
      - string
      - boolean

    # Enables Control Changes
    control: true

# UI JSON schemas (Optional)
ui_schemas:
  # Application Configuration Schema
  configuration: "ui_schemas/configuration.json"
  # IO Configuration Schema (specific for each importer_io item)
  io_configuration:
    default: "ui_schemas/io_default.json"

# Defaults (Runtime/Deployment)
defaults:
  # Application Configuration Default Values
  configuration: {}

  # System Settings
  system: {}
