# Include all Python source files (recursively) within the package directory
recursive-include ioriver *.py

# Include any additional files you want to include in the distribution
# include LICENSE
include README.md
include requirements.txt

# Include data files in the "data" folder (you can specify specific file types too)
# recursive-include data *

# Exclude certain files from being included in the package
exclude .pylintrc

# You can also use `global-include` to specify file types for all directories
# global-include *.txt *.md
