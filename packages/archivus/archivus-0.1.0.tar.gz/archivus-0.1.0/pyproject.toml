[project]
name = "archivus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eivind Teig", email = "eivind.teig@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
archivus = "archivus:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
