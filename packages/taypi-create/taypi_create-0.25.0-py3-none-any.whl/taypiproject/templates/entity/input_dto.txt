from pydantic import BaseModel, Field
from typing import Optional

# import value objects

class Input{{ pascal_case }}(BaseModel):
    example: str = Field(..., description="Example field")

class InputUpdate{{ pascal_case }}(BaseModel):
    example: str = Field(..., description="Example field")