[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "abe-redis"
version = "0.0.3"
description = "📮 A Redis-based abstracted backend (abe) for Slack MCP server message queue."
authors = [{ name = "Chisanan232", email = "chi10211201@cycu.org.tw" }]
requires-python = ">=3.12,<3.14"
readme = "README.md"
license = { file = "LICENSE" }
keywords = [
    "slack",
    "mcp",
    "mcp-server",
    "slack-mcp",
    "slack-mcp-server",
    "slack-event",
    "slack-event-handler",
    "message-queue",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Utilities",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
#    "Programming Language :: Python :: 3.14",  # TODO: Because of PyO3's maximum supported version (3.13)
]
dependencies = [
    "redis[hiredis]>=6.4.0",
    "slack-mcp>=0.0.1",
]

[project.urls]
Homepage = "https://github.com/Chisanan232/MCP-BackEnd-Message-Queue-Redis"
Repository = "https://github.com/Chisanan232/MCP-BackEnd-Message-Queue-Redis"
Documentation = "https://chisanan232.github.io/MCP-BackEnd-Message-Queue-Redis/"
Changelog = "https://chisanan232.github.io/MCP-BackEnd-Message-Queue-Redis/docs/next/changelog"
"Project Management" = "https://app.clickup.com/9018752317/v/f/90185744047/90184991515"

[project.entry-points."slack_mcp.backends.queue"]
redis = "slack_mcp_plugin.backends.queue.redis:RedisMessageQueueBackend"

[dependency-groups]
dev = [
    "pytest>=8.1.1,<9",
    "pytest-cov>=5.0.0,<8",
    "coverage~=7.10",
    "pytest-rerunfailures>=14.0,<17",
    "aiohttp>=3.12.13",
    "pytest-asyncio>=0.23.0,<2",
    "python-dotenv>=1.0.1,<2",
    "testcontainers[redis]>=4.0.0,<5",
]
pre-commit-ci = [
    "pre-commit>=3.5.0,<5",
    "pylint>=3.1.0,<4",
    "mypy>=1.2.0,<2",
]

[tool.uv]
default-groups = [
    "dev",
    "pre-commit-ci",
]

[tool.hatch.build.targets.sdist]
packages = ["slack_mcp_plugin"]

[tool.hatch.build.targets.wheel]
packages = ["slack_mcp_plugin"]
