[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "statsapi"
version = "1.7.2"
description = "POC package (beacon-only)"
authors = [{name = "Vishal Arora"}]
readme = "README.md"
requires-python = ">=3.6"

[tool.setuptools]
# This disables wheel building so pip always uses sdist (.tar.gz)
