[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mmaing_aesop"
version = "0.1.0"
description = "MMAING-AESOP: Early Warning System combining Rt and ML ensemble for outbreak detection."
authors = [
    { name = "Dérick Borges", email = "seudemail@example.com" }
]
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "pyod",
    "matplotlib",
    "scipy"
]

[project.urls]
Homepage = "https://pypi.org/project/mmaing_aesop/"
