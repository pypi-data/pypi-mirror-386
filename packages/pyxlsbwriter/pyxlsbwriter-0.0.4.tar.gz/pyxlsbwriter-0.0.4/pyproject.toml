[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyxlsbwriter"
version = "0.0.4"
authors = [
  { name="Krzysztof Duśko"},
]
description = "A Python library to write XLSB and XLSX files."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
]

[project.optional-dependencies]
test = [
    "pyodbc"
]
examples = [
    "memory-profiler"
]


