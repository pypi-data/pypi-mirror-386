# --- Conteúdo Padrão do ruff.toml ---
line-length = 125
extend-include = ["*.ipynb"]

[lint]
# select = [
#     "E",  # pycodestyle errors
#     "W",  # pycodestyle warnings
#     "F",  # pyflakes
#     # "I",  # isort
#     "UP", # pyupgrade
#     # Adicione outras categorias que desejar como padrão
# ]
ignore = []
# fixable = ["ALL"] # Descomente se quiser habilitar autofix para tudo

[format]
# quote-style = "double"
# indent-style = "space"
# skip-magic-trailing-comma = false
# line-ending = "lf"

# Adicione outras configurações que você usa por padrão
# [lint.isort]
# known-first-party = ["my_app"]