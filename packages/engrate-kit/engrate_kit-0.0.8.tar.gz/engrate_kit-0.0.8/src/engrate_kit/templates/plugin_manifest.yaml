name: "{{project_name}}"
author: "{{author}}"
description: "{{description}}"
product_category: "{{product_category}}"
extensions:
  markets:
    - "*"
plugin_metadata:
  display_name: "{{project_name}}"
  service_name: "{{canonical_name}}"
  url_prefix: "{{canonical_name}}"
  fav_icon: ""
  image: ""
  port: "8000"
  flavors:
    - "external-api"
  traits:
    - "storage"
