"""Monitor and collect static system information including OS details, hardware configuration, and boot time."""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/monitors/system.ipynb.

# %% auto 0
__all__ = ['get_static_system_info']

# %% ../../nbs/monitors/system.ipynb 3
import platform
import sys
import socket
import psutil
from datetime import datetime

# %% ../../nbs/monitors/system.ipynb 5
def get_static_system_info() -> dict:  # A dictionary containing static system information
    """Get system information that doesn't change during runtime."""
    
    try:
        hostname = socket.gethostname()
    except:
        hostname = "Unknown"

    return {
        'os': platform.system(),
        'os_version': platform.version(),
        'os_release': platform.release(),
        'architecture': platform.machine(),
        'processor': platform.processor() or "Unknown",
        'hostname': hostname,
        'python_version': sys.version.split()[0],
        'cpu_count': psutil.cpu_count(logical=False),
        'cpu_count_logical': psutil.cpu_count(logical=True),
        'boot_time': datetime.fromtimestamp(psutil.boot_time()).strftime('%Y-%m-%d %H:%M:%S')
    }
