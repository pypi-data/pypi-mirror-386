from setuptools import setup, find_packages

setup(
    name="myprog_lib",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Deepak M",
    description="A simple library for program name lookup",
    python_requires=">=3.6",
)