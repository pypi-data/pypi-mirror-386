FROM bhugo/killms:3.2.2

#FROM {REGISTRY}/ddfacet:{VERSION}-{BUNDLE_VERSION}

# {pre_install}

# RUN python{default_python_version} -mpip install --use-pep517 --no-cache-dir {package} {extra_deps}

# {post_install}

CMD ["kMS.py", "--help"]
