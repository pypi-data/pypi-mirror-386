[project]
name = "duper-python"
description = "The format that's super!"
dynamic = ["version"]
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Eric Rodrigues Pires", email = "eric@eric.dev.br" }]
readme = "README.md"
keywords = ["duper", "parser", "serialization", "pydantic", "fastapi"]
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Topic :: File Formats",
    "Framework :: FastAPI",
    "Framework :: Pydantic",
    "Framework :: Pydantic :: 2",
]
urls = { homepage = "https://github.com/EpicEric/duper", source = "https://github.com/EpicEric/duper" }

[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[tool.maturin]
python-source = "python"
module-name = "duper._duper"
features = ["pyo3/extension-module"]
bindings = "pyo3"
manifest-path = "duper-python/Cargo.toml"

[dependency-groups]
dev = [
    "fastapi[standard]>=0.119.0",
    "httpx>=0.28.1",
    "pytest>=8.4.2",
    "starlette>=0.48.0",
]
