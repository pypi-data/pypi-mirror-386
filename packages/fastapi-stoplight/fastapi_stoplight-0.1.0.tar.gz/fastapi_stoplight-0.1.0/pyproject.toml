[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fastapi-stoplight"
version = "0.1.0"
description = "一个将 FastAPI 与 Stoplight Elements 结合的库，用于快速生成美观、交互式的 API 文档"
readme = "README.md"

dependencies = [
    "fastapi[all]>=0.116.1",
    "uvicorn>=0.35.0",
]
