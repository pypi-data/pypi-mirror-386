[package]
name = "recorder-errors"
version = "0.1.0"
edition = "2021"
license = "MIT"
readme = "README.md"
description = "Shared error facade for codetracer recorders"
authors = ["CodeTracer Runtime Team"]
categories = ["development-tools"]
keywords = ["codetracer", "error-handling"]

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
