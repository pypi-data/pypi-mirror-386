[project]
name = "accuralai-canonicalize"
version = "0.1.0"
description = "Canonicalization helpers and plugins for the AccuralAI LLM pipeline."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "AccuralAI Maintainers" }]
dependencies = [
  "accuralai-core",
  "pydantic>=2.5",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-anyio",
  "pytest-mock",
]

[project.entry-points."accuralai_core.canonicalizers"]
"standard" = "accuralai_canonicalize.canonicalizer:build_standard_canonicalizer"

[project.urls]
homepage = "https://github.com/accuralai/accuralai-canonicalize"
repository = "https://github.com/accuralai/accuralai-canonicalize"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
