[project]
name = "parallel-processing"
version = "0.1.5"
description = ""
authors = [{ name = "Louis Nguyen", email = "louis.nguyen@qode.world" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.poetry]
packages = [{ include = "parallel_processing", from = "src" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
