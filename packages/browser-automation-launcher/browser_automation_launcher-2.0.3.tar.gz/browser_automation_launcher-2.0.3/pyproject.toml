[project]
name = "browser-automation-launcher"
version = "2.0.3"
description = "Browser Automation launcher"
readme = "README.md"
authors = [{ name = "Martian Bugs" }]
requires-python = ">=3.12"
dependencies = ["drissionpage>=4.1.1.2,<5.0.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/BrowserAutomationLauncher"]
