"""Compatibility tests for polyspark with external dependencies.

This directory contains tests that verify polyspark works correctly with:
- PySpark (pyspark package)
- Mock-Spark (mock-spark package)
- Pydantic (pydantic package)

These tests are run in separate environments with the respective dependencies installed.
"""
