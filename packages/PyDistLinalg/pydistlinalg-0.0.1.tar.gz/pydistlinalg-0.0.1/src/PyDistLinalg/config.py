# PyDistLinalg/config.py

DEFAULT_CHUNKS = 'auto' # 'auto' или кортеж (например, (1000, 1000))
# В Dask 'auto' обычно выбирает чанки размером около 128 МБ.
# Это важный параметр для производительности!