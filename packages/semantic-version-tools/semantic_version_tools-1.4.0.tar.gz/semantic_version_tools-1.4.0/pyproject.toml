[project]
name = "semantic-version-tools"
version = "1.4.0"
description = "Class to manage semantic versioning"
authors = [
    {name = "Romolo Politi",email = "romolo.politi@inaf.it"}
]
readme = "README.md"
dependencies = [
    "setuptools (>=80.9.0,<81.0.0)"
]


[tool.poetry]
packages = [{include = "semantic_version_tools", from = "src"}]
include = ["CHANGELOG.md"]
name="semantic-version-tools"
version="1.3.1"
description="Class to manage semantic versioning"
authors = [
    "Romolo Politi <romolo.politi@inaf.it>"
]

[tool.poetry.dependencies]
python = ">=3.10, <3.15"


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
