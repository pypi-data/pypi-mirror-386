query GetSDOs($schemaId: ID!, $ids: [ID!], $limit: Int, $offset: Int) {
  structuredDataObjects(
    schemaId: $schemaId,
    ids: $ids,
    limit: $limit,
    offset: $offset,
    orderBy: [{
       direction: asc,
       field: createdDateTime
    }]
  ) {
    ...SDOPage
  }
}
